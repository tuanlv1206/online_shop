- @page_title = t('shop.product_category.product_categories')
= content_for :header do
  %p.buttons
    = link_to t('shop.localisations.localisations') , [@product_category, :localisations], :class => 'button'
    = link_to t('shop.product_category.back_to_categories'), :product_categories, :class => 'button'
  %h2.products= t('shop.product_category.product_categories')
= render 'form'

