- @page_title = "#{t('shop.localisations.localisations')} - #{@product_category.name}"

= content_for :header do
  %p.buttons
    = link_to t('shop.localisations.back'), product_categories_path, :class => 'button'
    = link_to t('shop.localisations.new_localisation'), [:new, @product_category, :localisation], :class => 'button green'

  %h2.products= t('shop.localisations.localisations_of', name: @product_category.name)

.table
  %table.data
    %thead
      %tr
        %th{:width => '20%'}= t('shop.localisations.language')
        %th{:width => '50%'}= t('shop.products.name')
        %th{:width => '15%'}= t('shop.products.permalink')
    %tbody
      - if @localisations.empty?
        %tr.empty
          %td{:colspan => 4}= t('shop.localisations.no_localisations')
      - else
        - for localisation in @localisations
          %tr
            %td= localisation.locale
            %td= link_to localisation.name, edit_product_category_localisation_path(@product_category, localisation)
            %td= localisation.permalink
