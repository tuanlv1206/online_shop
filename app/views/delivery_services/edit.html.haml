- @page_title = t('shop.delivery_services.delivery_services')

= content_for :header do
  %p.buttons
    = link_to t('shop.delivery_services.prices'), [@delivery_service, :delivery_service_prices], :class => 'button'
    = link_to t('shop.delivery_services.back'), :delivery_services, :class => 'button'
  %h2.delivery_services= t('shop.delivery_services.delivery_services')

= render 'form'
