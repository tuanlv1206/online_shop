- @page_title = t('shop.products.products')

= content_for :header do
  %p.buttons
    = link_to t('shop.products.new_product'), :new_product, :class => 'button green'
    = link_to t('shop.products.import_products'), :import_products, :class => 'button gray'
  %h2.products= t('shop.products.products')

= render 'table', :products => @products

= paginate @products_paged
