- @page_title = t('shop.products.products')

= content_for :header do
  %p.buttons= link_to t('shop.products.back_to_products') , :products, :class => 'button'
  %h2.products= t('shop.products.products')

= render 'form'
