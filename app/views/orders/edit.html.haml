- @page_title = "#{t('shop.orders.edit_order')} ##{@order.number}"

= content_for :header do
  %p.buttons
    = link_to t('shop.orders.back_to_order'), @order, :class => 'button grey'
  %h2.orders "#{t('shop.orders.edit_order')} <b>##{@order.number}</b>"

= form_for @order, :html => {:class => 'orderForm'} do |f|
  = render 'shared/error_messages', object: f.object
  = render 'form', :f => f

  = field_set_tag  t('shop.orders.notes') do
    %dl
      %dd= f.text_area :notes, :class => 'text'

  = field_set_tag t('shop.orders.ordered_products'), :class => 'padded' do
    = render 'order_items_form', :f => f

  %p.submit
    = f.submit t('shop.orders.save_order') , :class => 'button green'
    = link_to t('shop.cancel'), @order, :class => 'button'
