- if object.errors.any?
  .errorExplanation
    %h2= "#{pluralize(object.errors.count, "error")} prohibited this #{object.class.name.split(/(?=[A-Z])/).join(" ")} from being saved"
    %ul
      - object.errors.full_messages.each do |msg|
        %li= msg
